@{
    IEnumerable<dynamic> dataList = Model;
    foreach (var item in dataList.Where(c => c.OrderItem == 1))
    {
        <el-option value="@(item.Name) desc" label="@(item.Remark)↓"></el-option>
        <el-option value="@(item.Name) asc" label="@(item.Remark)↑"></el-option>
    }
}


